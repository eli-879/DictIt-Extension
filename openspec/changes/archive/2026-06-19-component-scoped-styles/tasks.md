## 1. IncomingWordPanel

- [x] 1.1 Move all `.incoming-panel*` rules from `App.scss` into `src/components/IncomingWordPanel.scss`

## 2. VocabularyEntry

- [x] 2.1 Move all `.vocab-entry*` rules from `App.scss` into `src/components/VocabularyEntry.scss`

## 3. VocabularyList

- [x] 3.1 Create `src/components/VocabularyList.scss` and move `.vocab-list*` rules into it from `App.scss`
- [x] 3.2 Add `import "./VocabularyList.scss"` to `src/components/VocabularyList.tsx`

## 4. Cleanup

- [x] 4.1 Confirm `App.scss` contains only `.app` and `.btn*` rules and nothing else
